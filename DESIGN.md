# 设计系统与前端维护规范

本项目使用 **Tabler Core 1.5.0 的原生 DOM 样式组件**，通过共享 tokens 适配桌面、App 手机和 Pad。业务继续由现有 ES modules、DOM 和事件处理驱动，不绑定 Tabler JS；弹窗与折叠继续使用原生 `dialog`、`details`。

本轮重点改善题库、图谱筛选、实验室和公共按钮/表单，并重组教师端的课件助手、课件库和 Skill hub。旧 CSS 作为兼容层保留，不代表已完成全量清理，也不代表全部工作区经过手机或 Pad 原生体验认证。验收证据与未覆盖项见 [UX 设计诊断采纳与设计系统](docs/UX设计诊断采纳与设计系统.md)。

## 文件职责与本地构建

| 文件 | 职责 |
| --- | --- |
| `public/design-system.css` | 页面设计样式的统一入口，声明 cascade layers 并导入各层 |
| `public/design-tokens.css` | 字体、颜色、间距、控件、导航、内容宽度等共享变量，以及旧变量/Tabler 变量的兼容映射 |
| `public/design-controls.css` | 公共按钮、表单、图标、焦点与原生弹窗的产品规则 |
| `public/design-layout.css` | 应用外壳、侧栏、内容高度与共享响应式布局 |
| `public/design-system.js` | 保留原 DOM 的控件增强与工作区挂载，不替代业务状态和交互 |
| `public/*-ux.css` 等业务样式 | 对应工作区的结构布局；通过统一入口放入 `workspace` 层 |
| `scripts/build-design-system.mjs` | 从固定版本官方 CSS 中用 PostCSS 提取组件规则 |
| `public/vendor/tabler/controls.css` | 本地生成的官方组件样式，不直接手改 |
| `public/vendor/tabler/LICENSE` | 随生成资源保留的上游 MIT 许可证 |

安装仓库依赖后运行：

```bash
npm ci
npm run build:design
```

构建从 `node_modules/@tabler/core/dist/css/tabler.css` 读取官方源文件，使用 PostCSS 按组件选择器提取按钮、表单、输入组、徽标、提示、校验反馈和加载指示等规则，同时保留所需主题变量、媒体条件及加载动画。官方声明直接复制；产品定制在 `design` 层表达。生成结果为 `public/vendor/tabler/controls.css`，页面从本地加载，不依赖 CDN。

构建不引入 Tabler 页面框架、全局 reboot 或组件 JS。修改提取范围应修改构建脚本，修改产品视觉应修改共享 tokens/组件规则，不编辑生成文件。升级 Tabler 需同步精确版本、锁文件、来源与许可证说明，重新生成资源，并检查三种设备模式。单次构建成功仅说明生成流程可执行。

## 层叠与隐藏语义

统一入口的层级顺序必须为：

```css
@layer tabler, legacy, design, workspace;
```

| 层 | 允许承担的职责 |
| --- | --- |
| `tabler` | 官方组件基础规则，优先级低于已有产品与设备行为 |
| `legacy` | 已有业务和设备样式的隔离兼容；逐项迁移，不无依据整批删除 |
| `design` | 统一 tokens、公共控件和共享布局 |
| `workspace` | 题库、图谱、实验室等专属结构与必要的模式适配 |

正常声明按以上次序覆盖。基础库不能让业务按设备、角色或状态隐藏的控件重新显示。迁移后的组件仍需尊重 `hidden`、未打开的 `dialog`、折叠 `details` 及设备导航规则；新增 `display:flex/grid` 等声明时必须检查这些状态。

不要在入口外新增无 layer 的全局覆盖，也不要依靠不断加大选择器权重或 `!important` 解决层级错误。新功能应修改所属组件或工作区样式；**禁止继续追加 `*-fix.css`、`*-patch.css` 等补丁文件堆叠**。新独立业务模块如确需样式文件，必须职责单一并通过统一入口注册。

## 公共视觉与控件

共享数值以 `public/design-tokens.css` 为唯一维护来源。以下是当前基础档位，业务代码应引用变量：

| 类型 | 当前规范 |
| --- | --- |
| 字体 | 系统中文字体栈；辅助 12/13px，正文 14px，强调正文 16px，标题 20px |
| 间距 | 4、8、12、16、20、24、32px；用 `--ds-space-*` 表达 |
| 控件高度 | 桌面基础 36px；App、Pad、窄屏或粗指针触控基础 44px |
| 图标 | 16、20、24px；公共文字按钮与图标按钮分别引用对应 token |
| 圆角 | 控件 8px、面板 12px；引用 `--ds-radius*` |
| 侧栏 | 展开 224px，窄侧栏 56px；手机采用自己的导航结构 |
| 内容宽度 | 页面最大 1440px，阅读 680px，表单 640px；按内容类型选用 |

按钮采用 `.btn` 基础类，以及 `.btn-primary`、`.btn-ghost`、`.btn-danger`、`.btn-icon` 等语义变体；输入与选择使用 `.form-control`、`.form-select`。保留真实 `button`、`input`、`select`、`textarea`，不要用普通文本容器模拟控件。

同一任务区应能清楚辨认主要操作。文字与图标居中对齐，图标不被挤压，长中文文案允许合理换行或转入次级面板；不能通过缩小字号、裁掉关键文字或压低触控尺寸容纳更多按钮。只含图标的按钮需要可访问名称。检查正常、悬停、焦点、加载、禁用和错误状态；键盘焦点应可见，减少动画偏好应生效。

现有导航、课程卡、树节点、对话输入区和实验控制可能具有专属几何。自动添加基础类不等于完成其设计迁移；应先判断组件语义，避免把所有交互元素都改成相同的矩形按钮。

## 响应式与高度

新样式统一使用 **640 / 1024 / 1440 / 1920px** 响应边界；连续区间如下。原有兼容层的零散阈值暂时保留，修改对应区域时逐步归并，不宣称整个仓库已只剩四个边界。

| 内容视口宽度 | 设计原则 |
| --- | --- |
| 小于 640px | 紧凑单列，按任务顺序组织，次级操作使用可访问的弹层或折叠 |
| 640–1023px | 按可用空间收敛多栏；非 App 模式使用 56px 窄侧栏 |
| 1024–1439px | 可采用列表/详情或操作/预览双栏；默认展开侧栏 224px |
| 1440–1919px | 完整工作区布局，正文仍遵守阅读宽度，不无限拉长 |
| 至少 1920px | 增加外围留白，使用最大内容宽度，保持控件和文字比例 |

设备模式与宽度是两个维度：`data-display-mode="app"` 或 `"pad"` 决定外壳、导航和触控规则；媒体查询处理内容视口空间，不能只根据宽度把 Pad 当作手机。预览外壳的缩放也不等于内容视口改变，应检查 iframe 的实际布局尺寸。

外层拥有视口高度，Grid 把导航、工具栏和剩余内容区分配清楚；工作区继承可用高度。子面板通常需要 `min-width: 0`、`min-height: 0` 和 `minmax(0, 1fr)`，并为内容指定明确的滚动容器。不要让每个页面再猜一遍顶部占用、反复写 `100vh - 104px`。对话框为保留视口边距设置最大高度可以使用 `calc`。

宽屏采用双栏、窄屏采用堆叠或面板切换时，完整操作仍需可达。Pad 保留横竖屏，按比例组织侧栏、列表、详情与工具区；手机在小视口下检查固定底部操作、长列表和键盘弹出后的空间。

真实物理画布保持 **900×520 / 45:26**，通过容器等比适配。不能因预览区域采用 16:9 而拉伸画布，也不能在录制过程中随布局重置后备分辨率；需要验证实验运行、录像和离线导出各自的结果。

## 新功能与迁移验收

所有新功能和视觉迁移必须复用上述入口、tokens 与组件规则，并验证 **desktop / App / Pad**，包括适用的学生和教师角色。修改前先检查运行时挂载、已有监听和任务状态；迁移 DOM 应保留原节点的 ID、数据属性、表单行为和事件，不建立功能相同但状态分离的副本。

至少记录以下结果：

- 正常与长内容下的布局、字号、对齐、触控范围、滚动和操作可达性；覆盖 Pad 横竖屏及低高度视口。
- 加载、空态、失败、禁用、选择与清除等状态；隐藏控件没有被基础库或公共规则重新显示。
- 键盘导航、可见焦点、Escape/关闭行为及焦点恢复；原生弹窗和折叠只有一套行为控制。
- 模式切换后输入草稿、选中内容、会话和进行中任务的状态；依据现有契约验证，不因重排丢失。
- 受影响业务的关键流程：题库搜索/筛选/生成，图谱搜索/筛选/详情/导入任务，实验播放/参数/录制/导出。
- 实际运行的构建与相关回归，以及截图、日志或报告路径；分别标明本地静态、Mock 和真实上游范围。

截图或构建通过不能代替完整交互证据；部分页面检查也不能推导出全应用适配完成。每次交付应明确本次修改范围、已验证行为和仍未覆盖的部分。

## 教师创作工作流

课堂预览位于导航首位。课件助手统一原视频讲解、素材工坊和互动教材，沿用既有运行时；只将可实际调用的类型列为制作能力。平面几何使用已有直角三角形、圆与扇形模板，需明确说明支持范围；候选技术不等于已接入能力。

课件库按学科、类型、技术栈、标签、交互性与来源筛选。Lesson DSL、平面几何 visualArtifact 与可用导出内容需保持当前参数；保存成功以 IndexedDB 事务提交为准，明确存储范围为当前浏览器。手机搜索常显，其余筛选默认折叠；Pad 和桌面默认展开。

Skill hub 将教学技能与技术栈放入同页 tabs，沿用原有服务清单和技术目录；技能权限仍由服务端决定。App / Pad 设备框内不出现模式切换或检索连接状态，统一由外层右上角承载。
